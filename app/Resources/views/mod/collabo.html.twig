{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier un collaborateur</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_end(form) }}
            
                 
{% endblock %}

{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('#appbundle_collaborateur_nom').keyup(function() {
                this.value = this.value.toUpperCase();
            });
            $('#appbundle_collaborateur_prenom').keyup(function() {
                this.value = this.value.toUpperCase();
            });
        });
    </script>
{% endblock %} 
    